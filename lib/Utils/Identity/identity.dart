import 'package:flutter/material.dart';

class CustomTextWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text(
      '5400',
      style: TextStyle(color: Colors.grey.withOpacity(0.06)),
    );
  }
}
