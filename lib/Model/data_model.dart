// lib/Models/data_model.dart
class DataModel {
  final String? key; // For the X-axis label (e.g., date)
  final String? value; // For the Y-axis value (e.g., count)

  DataModel({this.key, this.value});
}
